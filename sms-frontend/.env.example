# Copy this file to .env and adjust if your backend runs somewhere other than
# the default local port. Vite only exposes variables prefixed with VITE_ to
# the browser.

VITE_API_URL=http://localhost:4000/api
